<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">CareerPlug</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
            <ul class="nav navbar-nav">
              <li><a href="#">All Jobs</a></li>
              <li><a href="#">Post a Job</a></li>
            </ul>
            <form class="navbar-form navbar-left" role="search">
              <div class="form-group">
                <input type="text" class="form-control" placeholder="Search Jobs">
              </div>
              <button type="submit" class="btn btn-default">Submit</button>
            </form>
            <li><%= link_to "Welcome #{current_user.first_name}", edit_user_registration_url %></li>
            <li> <%= link_to "Sign Out", destroy_user_session_url, method: :delete%></li>
        <%else %>
            <li><%= link_to "Sign In" , new_user_session_url%></li>
            <li><%= link_to "Sign Up", new_user_registration_url%></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
